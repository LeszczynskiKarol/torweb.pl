---
// src/pages/kontakt.astro

import Layout from '../layouts/Layout.astro';
import Container from '../components/ui/Container.astro';
import Button from '../components/ui/Button.astro';
import Card from '../components/ui/Card.astro';
import Badge from '../components/ui/Badge.astro';

const contactMethods = [
  {
    icon: '📱',
    title: 'Telefon',
    value: '+48 509 370 772',
    href: 'tel:+48509370772',
    description: 'Pon-Pt: 9:00-17:00'
  },
  {
    icon: '✉️',
    title: 'Email',
    value: 'kontakt@torweb.pl',
    href: 'mailto:kontakt@torweb.pl',
    description: 'Odpowiedź w 24h'
  },
  {
    icon: '📍',
    title: 'Biuro',
    value: 'Toruń, Kujawsko-Pomorskie',
    href: 'https://maps.google.com/?q=Toruń',
    description: 'Spotkania po umówieniu'
  }
];

const formServices = [
  'Strona internetowa',
  'Sklep internetowy',
  'Aplikacja webowa',
  'Optymalizacja SEO',
  'Wsparcie techniczne',
  'Inne'
];

const budgets = [
  'Do 5 000 zł',
  '5 000 - 10 000 zł',
  '10 000 - 25 000 zł',
  '25 000 - 50 000 zł',
  'Powyżej 50 000 zł'
];
---

<Layout
  title="Kontakt - Agencja interaktywna Toruń"
  description="Skontaktuj się z nami. Tworzenie stron internetowych Toruń. ✅ Darmowa wycena ✅ Spotkanie osobiste ✅ Tel: 509 370 772"
  keywords="kontakt torweb, agencja interaktywna toruń kontakt, tworzenie stron toruń kontakt"
>
  <!-- Hero Section -->
  <section class="hero">
    <Container>
      <div class="hero-content">
        <Badge variant="primary" class="hero-badge">
          💬 Porozmawiajmy o Twoim projekcie
        </Badge>
        
        <h1 class="hero-title">
          Skontaktuj się z <span class="gradient-text">nami</span>
        </h1>
        
        <p class="hero-description">
          Chętnie odpowiemy na wszystkie pytania i przygotujemy darmową wycenę.
          Możemy spotkać się osobiście w Toruniu lub porozmawiać online.
        </p>
      </div>
    </Container>
  </section>

  <!-- Contact Section -->
  <section class="contact section">
    <Container>
      <div class="contact-grid">
        <!-- Contact Form -->
        <div class="contact-form-wrapper">
          <Card class="form-card">
            <h2 class="form-title">Wypełnij formularz</h2>
            <p class="form-subtitle">Odpowiemy w ciągu 24 godzin</p>
            
            <form class="contact-form" action="/api/contact" method="POST">
              <div class="form-row">
                <div class="form-group">
                  <label for="name">Imię i nazwisko *</label>
                  <input 
                    type="text" 
                    id="name" 
                    name="name" 
                    required
                    placeholder="Jan Kowalski"
                  />
                </div>
                
                <div class="form-group">
                  <label for="company">Firma</label>
                  <input 
                    type="text" 
                    id="company" 
                    name="company"
                    placeholder="Nazwa firmy"
                  />
                </div>
              </div>
              
              <div class="form-row">
                <div class="form-group">
                  <label for="email">Email *</label>
                  <input 
                    type="email" 
                    id="email" 
                    name="email" 
                    required
                    placeholder="jan@example.com"
                  />
                </div>
                
                <div class="form-group">
                  <label for="phone">Telefon</label>
                  <input 
                    type="tel" 
                    id="phone" 
                    name="phone"
                    placeholder="+48 123 456 789"
                  />
                </div>
              </div>
              
              <div class="form-group">
                <label for="service">Czego potrzebujesz? *</label>
                <select id="service" name="service" required>
                  <option value="">Wybierz usługę</option>
                  {formServices.map(service => (
                    <option value={service}>{service}</option>
                  ))}
                </select>
              </div>
              
              <div class="form-group">
                <label for="budget">Budżet projektu</label>
                <select id="budget" name="budget">
                  <option value="">Wybierz orientacyjny budżet</option>
                  {budgets.map(budget => (
                    <option value={budget}>{budget}</option>
                  ))}
                </select>
              </div>
              
              <div class="form-group">
                <label for="message">Opis projektu *</label>
                <textarea 
                  id="message" 
                  name="message" 
                  rows="6" 
                  required
                  placeholder="Opisz swój projekt, cele biznesowe, oczekiwania..."
                ></textarea>
              </div>
              
              <div class="form-group checkbox-group">
                <label class="checkbox-label">
                  <input type="checkbox" name="nda" value="yes" />
                  <span>Potrzebuję podpisania NDA przed rozmową</span>
                </label>
              </div>
              
              <div class="form-group checkbox-group">
                <label class="checkbox-label">
                  <input type="checkbox" name="consent" value="yes" required />
                  <span>Wyrażam zgodę na przetwarzanie danych osobowych w celu odpowiedzi na zapytanie *</span>
                </label>
              </div>
              
              <Button type="submit" variant="primary" size="lg" fullWidth>
                Wyślij zapytanie
                <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
                  <path d="M2 10L17 2L13 18L11 11L2 10Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
              </Button>
            </form>
          </Card>
        </div>
        
        <!-- Contact Info -->
        <div class="contact-info">
          <!-- Quick Contact -->
          <div class="contact-methods">
            {contactMethods.map(method => (
              <a href={method.href} class="contact-method">
                <div class="method-icon">{method.icon}</div>
                <div class="method-content">
                  <h3>{method.title}</h3>
                  <p class="method-value">{method.value}</p>
                  <span class="method-description">{method.description}</span>
                </div>
              </a>
            ))}
          </div>
          
          <!-- Business Info -->
          <Card class="business-card">
            <h3>Dane firmy</h3>
            <div class="business-info">
              <div class="info-item">
                <span class="info-label">Nazwa:</span>
                <span class="info-value">TorWeb.pl</span>
              </div>
              <div class="info-item">
                <span class="info-label">NIP:</span>
                <span class="info-value">8792731215</span>
              </div>
              <div class="info-item">
                <span class="info-label">REGON:</span>
                <span class="info-value">123456789</span>
              </div>
              <div class="info-item">
                <span class="info-label">Adres:</span>
                <span class="info-value">ul. Przykładowa 1<br/>87-100 Toruń</span>
              </div>
            </div>
          </Card>
          
          <!-- Meeting Info -->
          <Card class="meeting-card">
            <h3>🤝 Spotkajmy się osobiście</h3>
            <p>
              Preferujesz rozmowę twarzą w twarz? Świetnie! 
              Możemy spotkać się w Toruniu na kawę i omówić szczegóły projektu.
            </p>
            <ul class="meeting-benefits">
              <li>Lepsze zrozumienie potrzeb</li>
              <li>Natychmiastowe odpowiedzi</li>
              <li>Budowanie relacji</li>
              <li>Możliwość podpisania umowy</li>
            </ul>
            <Button href="tel:+48509370772" variant="outline" fullWidth>
              Umów spotkanie
            </Button>
          </Card>
        </div>
      </div>
    </Container>
  </section>

  <!-- Map Section -->
  <section class="map-section">
    <Container>
      <div class="map-header">
        <h2>Gdzie nas znajdziesz?</h2>
        <p>Działamy w Toruniu i okolicach, ale realizujemy projekty dla firm z całej Polski</p>
      </div>
    </Container>
    
    <div class="map-wrapper">
      <div class="map-placeholder">
        <div class="map-overlay">
          <div class="map-info">
            <h3>📍 Toruń</h3>
            <p>Kujawsko-Pomorskie, Polska</p>
            <Button href="https://maps.google.com/?q=Toruń" target="_blank" variant="white" size="sm">
              Otwórz w Google Maps
            </Button>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- FAQ Section -->
  <section class="faq section bg-light">
    <Container size="md">
      <div class="section-header">
        <h2 class="section-title">
          Najczęściej zadawane <span class="gradient-text">pytania</span>
        </h2>
      </div>
      
      <div class="faq-list">
        <details class="faq-item">
          <summary>Jak szybko odpowiadacie na zapytania?</summary>
          <p>Staramy się odpowiedzieć w ciągu 24 godzin w dni robocze. 
          Na pilne sprawy najlepiej dzwonić bezpośrednio pod numer +48 509 370 772.</p>
        </details>
        
        <details class="faq-item">
          <summary>Czy mogę otrzymać darmową wycenę?</summary>
          <p>Tak, pierwsza wycena jest całkowicie bezpłatna i niezobowiązująca. 
          Po wypełnieniu formularza przygotujemy szczegółową wycenę z harmonogramem.</p>
        </details>
        
        <details class="faq-item">
          <summary>Czy możliwe jest spotkanie osobiste?</summary>
          <p>Oczywiście! Chętnie spotkamy się w Toruniu lub okolicach. 
          Możemy też zorganizować spotkanie online przez Zoom, Google Meet lub Teams.</p>
        </details>
        
        <details class="faq-item">
          <summary>Jakie informacje powinienem przygotować?</summary>
          <p>Przydatne będą: opis projektu, cele biznesowe, grupa docelowa, 
          przykłady stron które się podobają, orientacyjny budżet i termin realizacji.</p>
        </details>
        
        <details class="faq-item">
          <summary>Czy podpisujecie umowy NDA?</summary>
          <p>Tak, bez problemu podpisujemy umowy o zachowaniu poufności. 
          Możemy użyć Waszego wzoru lub przygotować własny.</p>
        </details>
      </div>
    </Container>
  </section>

  <!-- CTA Section -->
  <section class="cta">
    <Container>
      <div class="cta-content">
        <h2 class="cta-title">
          Nie znalazłeś odpowiedzi na swoje pytanie?
        </h2>
        <p class="cta-description">
          Zadzwoń do nas lub napisz maila. Chętnie rozwiejemy wszelkie wątpliwości.
        </p>
        <div class="cta-buttons">
          <a href="tel:+48509370772" class="cta-button phone">
            <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
              <path d="M2 3C2 2.44772 2.44772 2 3 2H6.15287C6.64171 2 7.0589 2.35341 7.13927 2.8356L7.87858 7.27147C7.95075 7.70451 7.73206 8.13397 7.3394 8.3303L5.79126 9.10437C6.90756 11.8783 9.12168 14.0924 11.8956 15.2087L12.6697 13.6606C12.866 13.2679 13.2955 13.0492 13.7285 13.1214L18.1644 13.8607C18.6466 13.9411 19 14.3583 19 14.8471V18C19 18.5523 18.5523 19 18 19H16C8.26801 19 2 12.732 2 5V3Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
            +48 509 370 772
          </a>
          <a href="mailto:kontakt@torweb.pl" class="cta-button email">
            <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
              <path d="M3 5C3 3.89543 3.89543 3 5 3H15C16.1046 3 17 3.89543 17 5V15C17 16.1046 16.1046 17 15 17H5C3.89543 17 3 16.1046 3 15V5Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              <path d="M3 5L10 10L17 5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
            kontakt@torweb.pl
          </a>
        </div>
      </div>
    </Container>
  </section>
</Layout>

<style>
  /* Hero Section */
  .hero {
    padding: var(--space-2xl) 0 var(--space-xl);
    background: linear-gradient(135deg, var(--color-light) 0%, var(--color-white) 100%);
    text-align: center;
  }

  .hero-content {
    max-width: 48rem;
    margin: 0 auto;
  }

  .hero-badge {
    margin-bottom: var(--space-md);
  }

  .hero-title {
    font-size: var(--text-4xl);
    font-weight: 800;
    line-height: 1.1;
    margin-bottom: var(--space-md);
  }

  .gradient-text {
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-secondary) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-description {
    font-size: var(--text-xl);
    color: var(--color-gray-600);
    line-height: 1.6;
  }

  /* Contact Section */
  .contact {
    padding: var(--space-3xl) 0;
  }

  .contact-grid {
    display: grid;
    grid-template-columns: 2fr 1.5fr;
    gap: var(--space-2xl);
  }

  /* Contact Form */
  .form-card {
    padding: var(--space-2xl);
    background: white;
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-lg);
  }

  .form-title {
    font-size: var(--text-2xl);
    margin-bottom: var(--space-sm);
  }

  .form-subtitle {
    color: var(--color-gray-600);
    margin-bottom: var(--space-xl);
  }

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
  }

  .form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-lg);
  }

  .form-group {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .form-group label {
    font-weight: 600;
    color: var(--color-gray-700);
    font-size: var(--text-sm);
  }

  .form-group input,
  .form-group select,
  .form-group textarea {
    padding: 0.75rem 1rem;
    background: var(--color-gray-50);
    border: 1px solid var(--color-gray-300);
    border-radius: var(--radius-lg);
    font-size: var(--text-base);
    font-family: inherit;
    transition: all var(--transition-fast);
  }

  .form-group input:focus,
  .form-group select:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: var(--color-primary);
    background: white;
    box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.1);
  }

  .form-group textarea {
    resize: vertical;
    min-height: 120px;
  }

  .checkbox-group {
    flex-direction: row;
    align-items: center;
  }

  .checkbox-label {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    cursor: pointer;
    font-size: var(--text-sm);
    color: var(--color-gray-700);
  }

  .checkbox-label input[type="checkbox"] {
    width: 18px;
    height: 18px;
    cursor: pointer;
  }

  /* Contact Info */
  .contact-info {
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
  }

  .contact-methods {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .contact-method {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    padding: var(--space-lg);
    background: white;
    border-radius: var(--radius-xl);
    border: 1px solid var(--color-gray-200);
    text-decoration: none;
    color: inherit;
    transition: all var(--transition-base);
  }

  .contact-method:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
    border-color: var(--color-primary);
  }

  .method-icon {
    font-size: 2.5rem;
    flex-shrink: 0;
  }

  .method-content h3 {
    font-size: var(--text-base);
    font-weight: 600;
    color: var(--color-gray-700);
    margin-bottom: 0.25rem;
  }

  .method-value {
    font-size: var(--text-lg);
    font-weight: 600;
    color: var(--color-primary);
    margin: 0;
  }

  .method-description {
    font-size: var(--text-sm);
    color: var(--color-gray-500);
  }

  /* Business Card */
  .business-card {
    padding: var(--space-xl);
    background: white;
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-sm);
  }

  .business-card h3 {
    font-size: var(--text-lg);
    margin-bottom: var(--space-md);
  }

  .business-info {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .info-item {
    display: flex;
    gap: var(--space-sm);
    padding: var(--space-sm) 0;
    border-bottom: 1px solid var(--color-gray-100);
  }

  .info-item:last-child {
    border-bottom: none;
  }

  .info-label {
    font-weight: 600;
    color: var(--color-gray-600);
    font-size: var(--text-sm);
    min-width: 80px;
  }

  .info-value {
    color: var(--color-gray-700);
    font-size: var(--text-sm);
  }

  /* Meeting Card */
  .meeting-card {
    padding: var(--space-xl);
    background: linear-gradient(135deg, rgba(37, 99, 235, 0.05) 0%, rgba(124, 58, 237, 0.05) 100%);
    border: 2px solid var(--color-primary);
    border-radius: var(--radius-xl);
  }

  .meeting-card h3 {
    font-size: var(--text-lg);
    margin-bottom: var(--space-sm);
  }

  .meeting-card p {
    color: var(--color-gray-600);
    margin-bottom: var(--space-md);
    line-height: 1.6;
  }

  .meeting-benefits {
    list-style: none;
    padding: 0;
    margin-bottom: var(--space-lg);
  }

  .meeting-benefits li {
    padding: var(--space-sm) 0;
    padding-left: 1.5rem;
    position: relative;
    color: var(--color-gray-700);
    font-size: var(--text-sm);
  }

  .meeting-benefits li::before {
    content: '✓';
    position: absolute;
    left: 0;
    color: var(--color-success);
    font-weight: 700;
  }

  /* Map Section */
  .map-section {
    padding: var(--space-3xl) 0;
    background: var(--color-gray-50);
  }

  .map-header {
    text-align: center;
    margin-bottom: var(--space-2xl);
  }

  .map-header h2 {
    font-size: var(--text-3xl);
    margin-bottom: var(--space-sm);
  }

  .map-header p {
    color: var(--color-gray-600);
    font-size: var(--text-lg);
  }

  .map-wrapper {
    width: 100%;
    height: 400px;
    position: relative;
    overflow: hidden;
  }

  .map-placeholder {
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    position: relative;
  }

  .map-overlay {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.3);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .map-info {
    text-align: center;
    color: white;
  }

  .map-info h3 {
    font-size: var(--text-2xl);
    margin-bottom: var(--space-sm);
  }

  .map-info p {
    font-size: var(--text-lg);
    margin-bottom: var(--space-lg);
    opacity: 0.9;
  }

  /* FAQ Section */
  .faq-list {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .faq-item {
    background: white;
    border: 1px solid var(--color-gray-200);
    border-radius: var(--radius-lg);
    overflow: hidden;
    transition: all var(--transition-base);
  }

  .faq-item[open] {
    box-shadow: var(--shadow-md);
    border-color: var(--color-primary);
  }

  .faq-item summary {
    padding: var(--space-lg);
    font-weight: 600;
    cursor: pointer;
    list-style: none;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .faq-item summary::after {
    content: '+';
    font-size: var(--text-2xl);
    color: var(--color-primary);
    transition: transform var(--transition-fast);
  }

  .faq-item[open] summary::after {
    transform: rotate(45deg);
  }

  .faq-item p {
    padding: 0 var(--space-lg) var(--space-lg);
    color: var(--color-gray-600);
    line-height: 1.6;
  }

  /* CTA Section */
  .cta {
    padding: var(--space-3xl) 0;
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-secondary) 100%);
    color: white;
  }

  .cta-content {
    text-align: center;
    max-width: 48rem;
    margin: 0 auto;
  }

  .cta-title {
    font-size: var(--text-3xl);
    margin-bottom: var(--space-md);
  }

  .cta-description {
    font-size: var(--text-lg);
    opacity: 0.95;
    margin-bottom: var(--space-xl);
    line-height: 1.6;
  }

  .cta-buttons {
    display: flex;
    gap: var(--space-md);
    justify-content: center;
    align-items: center;
  }

  .cta-button {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 1rem 2rem;
    background: white;
    color: var(--color-primary);
    font-size: var(--text-lg);
    font-weight: 600;
    text-decoration: none;
    border-radius: var(--radius-lg);
    transition: all var(--transition-fast);
  }

  .cta-button:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }

  .cta-button.phone {
    background: rgba(255, 255, 255, 0.15);
    color: white;
    border: 2px solid white;
  }

  .cta-button.phone:hover {
    background: white;
    color: var(--color-primary);
  }

  /* Utils */
  .section {
    padding: var(--space-3xl) 0;
  }

  .bg-light {
    background: var(--color-light);
  }

  .section-header {
    text-align: center;
    margin-bottom: var(--space-3xl);
  }

  .section-title {
    font-size: var(--text-3xl);
    font-weight: 700;
    margin-bottom: var(--space-md);
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .contact-grid {
      grid-template-columns: 1fr;
    }

    .form-row {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 768px) {
    .cta-buttons {
      flex-direction: column;
    }

    .contact-method {
      flex-direction: column;
      text-align: center;
    }
  }
</style>

<script>
  // Form handling
  const form = document.querySelector('.contact-form') as HTMLFormElement;
  
  form?.addEventListener('submit', async (e) => {
    e.preventDefault();
    
    const formData = new FormData(form);
    const button = form.querySelector('button[type="submit"]') as HTMLButtonElement;
    
    // Add loading state
    button.disabled = true;
    button.textContent = 'Wysyłanie...';
    
    try {
      // Here you would send the form data to your backend
      // const response = await fetch('/api/contact', {
      //   method: 'POST',
      //   body: formData
      // });
      
      // Simulate API call
      await new Promise(resolve => setTimeout(resolve, 2000));
      
      // Show success message
      alert('Dziękujemy za wiadomość! Odpowiemy w ciągu 24 godzin.');
      form.reset();
      
    } catch (error) {
      alert('Wystąpił błąd. Spróbuj ponownie lub zadzwoń do nas.');
    } finally {
      button.disabled = false;
      button.innerHTML = `
        Wyślij zapytanie
        <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
          <path d="M2 10L17 2L13 18L11 11L2 10Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      `;
    }
  });
</script>